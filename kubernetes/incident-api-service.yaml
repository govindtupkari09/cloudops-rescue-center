apiVersion: v1
kind: Service
metadata:
  name: incident-api
  namespace: cloudops
spec:
  selector:
    app: incident-api
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP
